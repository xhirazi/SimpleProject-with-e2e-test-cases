# Simple Project with e2e test cases

## To Run App

- yarn && cd ios && bundle install && pod install

## To Run E2E test cases run

- yarn iosDebugBuild : to build
- yarn iosDebugTest : to run test cases
